export const environment = {
  production: true,

  // creamos la base de la URL (igual en todas las peticiones HTTP) 
  // copiamos el URL de Producción desde Heroku
  base_url: 'https://adminpro-backend-jla.herokuapp.com/api'
};
